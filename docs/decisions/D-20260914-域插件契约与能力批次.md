# D-20260914-graft 域插件契约与能力批次（GPT 方向咨询裁决）

> 执行者：bsp（Hermes BioScientist Agent）· 日期：2026-09-14
> 咨询通道：网页版 ChatGPT（自包含提示词 = `D:\Program\hermes\temp\graft-gpt-direction-brief.md`）
> 原始回复存档：`D:\Program\hermes\temp\graft-gpt-direction-reply.md`（15,559 字符）
> 裁决纪律：GPT 无本机上下文，其**事实断言必须本机核验后才采纳**；主张按「采纳 / 修正 / 拒绝」三分类，逐条给依据。

## 一、结论概览

GPT 的总体判断（「graft 已跨过『工具能跑』进入『语义不能错』阶段；接下来最值钱的不是堆 modality，
而是把『候选 → 比较 → 风险解释 → 可审计决策』闭环做严」）与本项目哲学一致，**采纳**。
它给出的优先级为 P0 rank + 脱靶语义层 → P1 碱基编辑 → P2 验证规划 → P3 其余，本裁决**基本采纳**，
仅对 A/B 的先后做了不影响实施的微调。

## 二、逐条裁决

### A. 直接采纳（改变/细化实施计划）

| # | GPT 主张 | 裁决 | 落点 |
|---|---|---|---|
| 1 | 脱靶返回值必须带 `search_completeness`（mismatch=searched；DNA/RNA bulge / 结构变异 / 样本变异=not_searched；**枚举而非布尔**） | 采纳 —— 比任何「风险分」信息量都大，且天然防止「未检出=安全」的推理 | 批次 C：`graft_offtarget` 返回体新增该字段（枚举值 `searched`/`not_searched`/`not_applicable`） |
| 2 | API 里**不允许存在** `safe: true`，也不允许裸 `risk: "low"`；须给 `assessment.safety_conclusion = "not_supported"` + 理由 | 采纳 —— 现有的 `interpretation_boundary` 是自然语言，补机器可读字段才能被下游程序化强制 | 批次 C：新增 `assessment` 结构化块；测试断言其存在且取值受控 |
| 3 | `graft_rank` 三种 operator：硬约束 / 字典序 / Pareto；秩是 policy-dependent，返回体带 `policy_id`+`policy_digest`+`pareto_front`；**禁止默认隐形权重** | 采纳 —— 与我原设计的「声明式策略」一致，`policy_digest` 是新增的加分项（让「昨天 B 今天 D」可归因到策略本身） | 批次 C：`graft_rank` 返回值与 EditPlan `ranking_policy` 字段按此定稿 |
| 4 | `bio_crispr_guide` 的消解方式：**不删**、定义 capability tier（Tier 0 轻量 / Tier 1 graft_design / Tier 2 graft_offtarget+rank），并让旧工具返回 `scope: "lightweight_heuristic"` + `advanced_designer_available` —— **保留行为、只改语义标签** | 采纳 —— 比我此前「改 description 文案」更彻底，且对既有用户零破坏 | 批次 B 补做：genie `bio_crispr_guide` 返回值加标签字段 + persona 写明 tier 语义 |
| 5 | `BaseEditorProfile` 五层（targeting / chemistry / activity / evidence / applicability），把 **geometry 与 efficiency 彻底拆开**；第一版**禁止**输出「预计编辑效率 73%」 | 采纳 —— 与我的「verified + 未核标注」一致，且 `applicability.extrapolation_warning` 是好的显式字段 | 批次 D 设计定稿（profile 加 `core_window`/`broader_observed_window`/`boundary_semantics: not_absolute`） |
| 6 | **`EditStrategy` 抽象**：多 guide 不能只是 `Guide[]`（single_cut/paired_cut/deletion_pair/paired_nickase/multiplex/base_edit/prime_edit/HDR）；双 guide 风险需 `pairwise_event_analysis`，不是单 guide 风险相加 | 采纳（新抽象，我原计划缺失）—— 但要排在碱基编辑**之后**（它是重架构，而 CBE/ABE 是直接用户价值） | 新增批次 E′（路线图）；批次 C/D 的 EditPlan schema 预留 `strategy` 字段 |
| 7 | **negative evidence semantics**：全系统强制区分 `0` / `null` / `not_searched` / `not_applicable` / `unknown` | 采纳 —— 与本项目「未检出 ≠ 安全」同源，且是可测试的 schema 纪律 | 批次 C + `docs/ARCHITECTURE.md` 增「证据语义」节；测试加断言 |
| 8 | 强制 reference provenance：assembly / annotation release / sequence digest | 采纳（EditPlan 已有 `reference` 字段，缺强制校验） | 批次 C：`graft_plan_save(new)` 对 `reference.sequence_hash` 缺失给显式 warning（不阻塞） |
| 9 | transcript-specific consequence（同一编辑在不同转录本可 synonymous/missense 并存）；`intergenic ≠ harmless`；strand semantics 要同时给 guide strand / edited physical strand / reference 表示（C→T 在参考正链可能是 G→A） | 采纳 —— 三条都是我在碱基编辑设计里没写清的坑 | 批次 D 的 `graft_base_edit` 返回体与 skill 硬规则 |
| 10 | 用 `EditOutcomeMetrics`（metric + numerator + denominator + assay + value）替代裸 `efficiency: 43`；「高效」在插件里应禁用裸词 | 采纳（作为文档/rigor 条款；不删工具返回里的 score vector 字段——它们是**事实值**，不是结论） | persona 编辑域段 + skill + 报告模板 |
| 11 | 验证方案用 `ValidationRequirement[]`（required / recommended / conditional）+ 植物额外项（嵌合/倍性/可遗传/载体残留） | 采纳接口、实现留后 | EditPlan 0.2 预留 `validation_plan` 结构；批次 F 实现 |

### B. 修正后采纳

| # | GPT 主张 | 裁决与理由 |
|---|---|---|
| 12 | 优先级把 `graft_rank` 排第一、脱靶语义层并列 | **修正**：同批实施（批次 C），但内部顺序**先 A（语义层）后 B（rank）**。依据：E2E 实测显示当前脱靶返回值的信息缺口（缺 search_completeness、缺 per-guide 汇总）直接构成「结论被误读」的风险，属正确性问题；rank 属流程审计问题。两者同批交付，故排序差异不影响排期 |
| 13 | 验证模块（E）排第四但「接口提前设计」 | 采纳其「提前设计接口」的一半：`validation_plan` 结构化字段随 EditPlan 0.2 落地；**实现**仍留在后续批次 |
| 14 | roadmap：v0.x（rank+offtarget）→ v0.x+1（base editing）→ v0.x+2（strategy）→ v0.x+3（validation） | 采纳，与我的批次 A/B/C/D 对齐；**新增批次 E′ = EditStrategy/多 guide**，排在碱基编辑之后 |
| 15 | 「ML prediction 最后接入，不是最先」 | 采纳（与原计划一致：一律 external provider，不 bundle） |

### C. 未能证实 / 拒绝

| # | GPT 主张 | 核验结果与裁决 |
|---|---|---|
| 16 | 「Cas-OFFinder 官方 changelog 把 DNA/RNA bulge 的原生支持放在 **3.0.0**」 | **部分可证、关键点未证实**：本机核验 GitHub releases —— 确有 `3.0.0b3`（beta，2021-07-29，含 Windows 资产 `cas-offinder_windows_x86_64.zip`，注意与 2.4.1 的 `x86-64` 连字符命名不同），但**该 release 无 release notes**；master README 的 changelog 止于 2.3，且明确写「bulge 需用独立包装脚本 cas-offinder-bulge」。→ **不据此改代码**：维持「bulge 不支持 + 响亮报错」现状；把 3.0.0b3 与资产命名差异记入待评估（见 `docs/PROFILES-TODO.md`）。**但其操作性推论正确且已实现**：v2.4.1 的「未检出」不能推出「无 bulge 脱靶」 |
| 17 | 建议 `graft_rank` 输出「Tier 1 — Pareto-optimal candidates」式 UI 文案 | 采纳为**返回字段**（`pareto_front`/`tiers`），不采纳为「由插件生成人类可读解释」——解释仍由 agent 按证据撰写（两层生成契约） |

## 三、对本计划批次的具体影响

1. **批次 B（进行中）补一项**：`bio_crispr_guide` 返回值加 `scope`/`advanced_designer_available` 标签（裁决 4）。
2. **批次 C 设计定稿**：`search_completeness`（1）、`assessment.safety_conclusion`（2）、rank 三 operator + policy_digest（3）、证据语义枚举（7）、reference 校验（8）。
3. **批次 D 设计定稿**：BaseEditorProfile 五层 + geometry/efficiency 分离 + 无效率预测（5）；转录本/链向/intergenic 三条硬规则（9）；EditOutcomeMetrics 文档条款（10）。
4. **新增批次 E′**：`EditStrategy` + 多 guide/pairwise off-target（6）。
5. **批次 F**：`ValidationRequirement[]`（11）。

> 本裁决不改变批次 A/B 的已交付内容；B 仅追加一个标签字段。
